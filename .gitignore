node_modules/
dist/
.env
.env.local
*.sqlite
*.sqlite-journal
*.sqlite-shm
*.sqlite-wal
*.log
.DS_Store
# Ignore the contents of Docs/ but keep the BookStack API spec — it's read
# at runtime by src/catalog.ts and copied into the Docker image at build time.
Docs/*
!Docs/BookStack_API.json
